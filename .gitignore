.DS_Store
*.app/
*.bak

# update-signing private keys must NEVER be committed (they live offline, e.g.
# ~/.config/printer-keepalive/update.key). Belt-and-suspenders even though
# they're out of tree.
*.key
*.pem
